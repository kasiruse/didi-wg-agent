services:
  wg-agent:
    build: .
    container_name: didi-wg-agent
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./sessions:/app/sessions
      - ./config.yaml:/app/config.yaml
    environment:
      - TZ=Europe/Berlin
